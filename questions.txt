Q. Check kernel routing information
    A.  # route -n
        # netstat -r
        # ip route

Q. File that specify default gateway
    A.  /etc/sysconfig/network

Q. Difference between update and upgrade
    A. Upgrade deletes the old packages whereas Update will keep the old packages so that we can rollback
